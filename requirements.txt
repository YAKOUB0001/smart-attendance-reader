# المكتبات المطلوبة لتشغيل البرنامج

# توليد ملفات الإكسل
openpyxl>=3.1.0

# تشغيل الواجهة (HTML/CSS/JS) داخل نافذة برنامج مكتبي حقيقية
pywebview>=4.4

# ملاحظات:
# - على ويندوز، pywebview يستخدم مكوّن Edge WebView2 المدمج غالبًا بالنظام،
#   وإن لم يكن موجودًا سيطلب من المستخدم تثبيته تلقائيًا (مرة واحدة فقط).
# - على لينكس، قد تحتاج: sudo apt install python3-gi gir1.2-webkit2-4.0

# ستُضاف المكتبات التالية عند بناء وحدة OCR في الخطوة القادمة:
# paddleocr
# paddlepaddle
# opencv-python
